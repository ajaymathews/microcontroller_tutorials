IN UNION THE SIZE OF THE MAXIUM DATA TYPE IS ALOCATED IF IT IS ARRRAY SIZE OF ARRAY IS ALLOCATED.
AND IN CASE OF A SINGLE VARIABBLE ONLY ONE SPACE IS ALLOCATED , IF WE TRY TO DECALRE TWO VARIABLES WITH
TWO VALUES ONLY ONE(THE LAST ONE), IS ASSIGNED. BUT THE SAME VALUE CAN BE CALLED BY BOTH THE NAMES.
IF WE PRINT THOSE TWO VALUES WE ONLY GET LAST VALUE IN BOTH VARIABLES.